<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="5dp">
        <EditText android:id="@+id/start_time"
            android:hint="Tap to select start time."
            android:layout_width="match_parent"
            android:maxLength="5"
            android:editable="false"
            android:inputType="@null"   android:clickable="true"


            android:layout_height="wrap_content"/>

        <EditText android:id="@+id/end_time"
            android:hint="Tap to select end time."
            android:layout_width="match_parent"
            android:layout_below="@+id/alertname"
            android:lines="1"
            android:maxLength="5"

            android:editable="false"
            android:clickable="true"
            android:layout_height="wrap_content"/>

        <Button
            android:id="@+id/alertsubmit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignLeft="@+id/layalert"
            android:layout_below="@+id/toggleenable"
            android:background="@drawable/capsulshapebutton"
            android:gravity="center"
            android:padding="10dp"
            android:layout_gravity="center_horizontal"
            android:text="@string/Submit"
            android:textColor="@color/white"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </LinearLayout>
</android.support.constraint.ConstraintLayout>