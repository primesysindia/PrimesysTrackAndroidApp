<?xml version="1.0" encoding="utf-8"?><manifest xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools" package="com.primesys.VehicalTracking">

<!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->

<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.GET_ACCOUNTS" />
<uses-permission android:name="android.permission.USE_CREDENTIALS" />
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
<uses-permission android:name="package.name.permission.MAPS_RECEIVE" />
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

<uses-feature android:name="android.hardware.camera" android:required="true" />

<permission android:name="com.primesys.VehicalTracking.permission.MAPS_RECEIVE"
    android:protectionLevel="signature" />

<uses-feature android:glEsVersion="0x00020000" android:required="true" />

<application android:name="android.support.multidex.MultiDexApplication" android:allowBackup="true"
    android:icon="@drawable/ic_icon" android:label="@string/appllication_name" android:theme="@style/AppTheme.NoActionBar"
    tools:replace="android:icon,android:label,android:theme">
    ///add this line
    <activity android:name=".Home" android:label="@string/toolbar_name" android:theme="@style/AppTheme.NoActionBar"></activity>

    <meta-data android:name="com.google.android.geo.API_KEY" android:value="AIzaSyAyPQ7h3-MBB3IAOAMACNJJmhKl2LvtpkY" />

    <!-- For Live Relese VTS    android:value="AIzaSyAyPQ7h3-MBB3IAOAMACNJJmhKl2LvtpkY" /> -->


    <!-- for DEbug VTS  android:value="AIzaSyCsUL2QWb_jiO2DhXcq1iTWk9ErLOrmyro" /> -->


    <!--
            <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/google_maps_key" />
        -->
    <meta-data android:name="com.google.android.gms.version" android:value="@integer/google_play_services_version" />
    <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/facebook_app_id" />

    <uses-library android:name="com.google.android.maps" android:required="true" />

    <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
    <!--
            <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        -->
    <activity android:name=".ShowMap" android:label="@string/toolbar_name"></activity>
    <activity android:name=".LoginActivity" android:theme="@style/AppTheme.NoActionBar">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:name=".SignupActivity" android:label="@string/toolbar_name" android:theme="@style/AppTheme.NoActionBar"></activity>
    <activity android:name=".Forget_password" android:label="@string/toolbar_name" android:theme="@style/AppTheme.NoActionBar"></activity>
    <activity android:name=".googlelogin.ChooserActivity" android:label="@string/toolbar_name" />
    <activity android:name=".googlelogin.SignInActivity" android:label="@string/toolbar_name"
        android:theme="@style/ThemeOverlay.MyNoTitleActivity" />
    <activity android:name=".googlelogin.SignInActivityWithDrive" android:label="@string/toolbar_name"
        android:theme="@style/ThemeOverlay.MyNoTitleActivity" />
    <activity android:name="com.facebook.FacebookActivity" android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
        android:label="@string/toolbar_name" android:theme="@android:style/Theme.Translucent.NoTitleBar" />

    <!--
        <receiver
            android:name=".LocationReceiver"
            android:enabled="true"
            android:exported="true" >
        </receiver>
        -->


    <!-- Broadcast Receiver for check the internet connection is on / off -->
    <receiver android:name=".InternetConnectionReceiver">
        <intent-filter>
            <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
        </intent-filter>
    </receiver>


    <activity android:name=".TrackSwitch" android:label="@string/toolbar_name" android:theme="@style/AppTheme.NoActionBar"></activity>
    <activity android:name=".UserProfileActivity" android:label="@string/toolbar_name" android:theme="@style/AppTheme.NoActionBar"></activity>
     <activity
            android:name=".DateTimeActivity"
            android:label="@string/toolbar_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>
</application>

</manifest>

